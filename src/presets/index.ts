import { cubePresets } from './cube'
import { prismPresets } from './prism'
import { triangularPresets } from './triangular'
import { cylinderPresets } from './cylinder'
import { conePresets } from './cone'
export const cuttingPresets = [...cubePresets, ...prismPresets, ...triangularPresets, ...cylinderPresets, ...conePresets]
